1. **抽取仪式页3秒提示优化**

   * 修改 `draw.wxml` 中的倒计时显示部分，添加绝对定位

   * 修改 `draw.wxss` 中的 `.countdown-container` 样式，使用 `position: fixed` 使其悬浮在页面上方

   * 调整倒计时的位置，使其居中显示在页面上方，不影响其他元素

   * 确保倒计时显示时不会导致页面其他元素移动或晃动

2. **脊兽详情页rank排名位置调整**

   * 修改 `knowledge/detail.wxml`，将rank排名从基本信息部分移除

   * 在图片容器中添加rank排名显示

   * 修改 `knowledge/detail.wxss`，使用绝对定位将rank排名固定在图片框的右下角

   * 调整rank排名的样式，使其与图片背景协调

3. **预期效果**

   * 抽取仪式页：3秒倒计时提示悬浮在页面上方，不影响其他元素，页面不再晃动

   * 脊兽详情页：rank排名显示在图片框的右下角，与图片融合，布局更加紧凑美观

4. **修改步骤**

   * 首先修改抽取仪式页的倒计时样式

   * 然后修改脊兽


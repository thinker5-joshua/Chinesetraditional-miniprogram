1. **修改压缩工具以支持尺寸调整和指定质量比**

   * 更新 `compress-images/compress-images.js` 文件，添加尺寸调整功能

   * 使用 Canvas 实现将图片调整到 600\*900 尺寸

   * 支持 80% 质量比的图片压缩

   * 添加新的海报背景图 file ID 到压缩列表

2. **实现步骤**

   * 在 `compressImage` 函数中，使用 Canvas 绘制并调整图片尺寸

   * 设置 Canvas 尺寸为 600\*900

   * 使用 `drawImage` 方法将图片绘制到 Canvas 上，实现尺寸调整

   * 使用 `canvasToTempFilePath` 将 Canvas 转换为临时图片文件

   * 使用 `wx.compressImage` 以 80% 质量比压缩图片

   * 保存压缩后的图片到相册

3. **测试和验证**

   * 运行压缩工具，验证新的海报背景图是否能被正确压缩

   * 检查压缩后的图片尺寸是否为 600\*900

   * 验证压缩质量是否为 80%

4. **预期结果**

   * 新的神兽海报背景图将被压缩为 600\*900 尺寸，80% 质量比

   * 压缩后的图片将保存在本地相册中，便于后续使用

   * 压缩工具


我已经修复了守护神兽抽取结果页中返回按钮无法正常工作的问题，主要包括以下内容：

1. **检查问题**：
   - 查看了result.wxml文件，确认返回按钮的bindtap事件正确绑定了goBack函数
   - 检查了result.js文件，发现goBack函数使用了wx.navigateTo来跳转首页，这是不正确的

2. **修复问题**：
   - 将goBack函数中的wx.navigateTo改为wx.redirectTo
   - wx.redirectTo会关闭当前页面并跳转到首页，而wx.navigateTo是保留当前页面并跳转到新页面
   - 确保返回按钮点击后能正确返回守护神兽的首页

3. **验证修复**：
   - 检查了返回按钮的bindtap事件绑定
   - 确认wx.redirectTo的使用场景正确
   - 确保修改后的代码逻辑合理

通过这个修改，守护神兽抽取结果页中的返回按钮应该能够正常工作，点击后会正确返回守护神兽的首页。
我将修改以下文件中的分享函数，移除imageUrl参数，使分享时显示当前页面的截图：

1. **卦学习页** (`/subgames/64Hexagrams/pages/hexagram-list/hexagram-list.js`)
   - 移除 `onShareAppMessage` 函数中的 `imageUrl` 参数
   - 移除 `onShareTimeline` 函数中的 `imageUrl` 参数

2. **卦详情页** (`/subgames/64Hexagrams/pages/hexagram-detail/hexagram-detail.js`)
   - 移除 `onShareAppMessage` 函数中的 `imageUrl` 参数
   - 移除 `onShareTimeline` 函数中的 `imageUrl` 参数

修改后，微信小程序会默认截取当前页面的顶部区域作为分享图片，展示更真实的页面内容，提升分享效果。
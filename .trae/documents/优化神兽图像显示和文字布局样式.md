# 优化神兽图像显示和文字布局样式

## 1. 优化神兽图像显示

### 1.1 增大神兽图像直径
- 修改 `poster.js` 中第163-165行的参数：
  - 将 `beastImageWidth` 和 `beastImageHeight` 从400px改为420px
  - 将 `circleRadius` 从200px改为210px（直径420px）

### 1.2 移除圆圈边框颜色
- 删除或注释掉 `poster.js` 中以下三处的边框绘制代码：
  - 第178-183行（默认图片绘制中的边框）
  - 第225-230行（云图片加载中的边框）
  - 第291-296行（本地图片加载中的边框）

### 1.3 增强圆圈周围发光效果
- 修改 `poster.js` 中径向渐变的参数：
  - 将渐变半径范围从230px扩大到250px
  - 增强渐变颜色透明度，从0.3调整为0.5
  - 应用到以下三处：
    - 第169-176行（默认图片绘制中的发光效果）
    - 第216-223行（云图片加载中的发光效果）
    - 第282-289行（本地图片加载中的发光效果）

## 2. 调整文字布局和样式

### 2.1 守护能力样式
- 确认 `poster.js` 第345行的字体设置已经是加粗的（bold 28px KaiTi, serif），无需修改

### 2.2 调整守护能力与其他文字的间距
- 修改 `poster.js` 中第354行和第360行的Y坐标：
  - 将契主特质的Y坐标从710调整为720px（间距从80px调整为90px）
  - 将专属箴言的Y坐标从750调整为760px

### 2.3 调整文字颜色和标题
- 修改 `poster.js` 中第352行和第358行的颜色：
  - 将契主特质和专属箴言的颜色从`#8B4513`改为`#000000`
- 修改 `poster.js` 第360行的标题：
  - 将"专属箴言"改为"守护箴言"

## 3. 验证修改效果
- 确保所有修改都已正确应用
- 检查代码语法是否正确
- 运行小程序查看海报生成效果
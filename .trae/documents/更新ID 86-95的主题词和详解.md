## 更新剩余所有菜根谭摘录的主题词和详解计划

### 任务分析
当前已完成ID 1-85的更新，剩余ID 86-378共293则摘录需要更新主题词和详解。

### 解决方案
1. **继续使用批量更新方法**：每次更新10条记录，保持与之前一致
2. **按顺序生成更新脚本**：从ID 86开始，依次更新到ID 378
3. **保持主题词和详解质量**：主题词8-10字，详解包含准确译文和深入赏析
4. **验证更新结果**：每次更新后检查内容是否正确

### 具体步骤
1. 创建`update_batch_86-95.js`脚本，更新ID 86-95
2. 运行脚本执行更新并验证结果
3. 创建`update_batch_96-105.js`脚本，更新ID 96-105
4. 运行脚本执行更新并验证结果
5. 以此类推，直到完成ID 378的更新

### 预期结果
所有ID 86-378的摘录都将有符合要求的主题词和完整的译文赏析，质量与之前更新的一致。

### 开始执行
现在开始创建并运行ID 86-95的更新脚本。
我将修改以下8个页面的分享函数，移除imageUrl参数，使分享时显示当前页面的截图：

1. **二十八星宿首页** (`/subgames/28Stars/index.js`)

   * 移除 `onShareAppMessage` 函数中的 `imageUrl` 参数

   * 移除 `onShareTimeline` 函数中的 `imageUrl` 参数

2. **看星象页** (`/subgames/28Stars/pages/constellation-map/constellation-map.js`)

   * 移除 `onShareAppMessage` 函数中的 `imageUrl` 参数

   * 移除 `onShareTimeline` 函数中的 `imageUrl` 参数

3. **去识星页** (`/subgames/28Stars/pages/star-matching/star-matching.js`)

   * 移除 `onShareAppMessage` 函数中的 `imageUrl` 参数

   * 移除 `onShareTimeline` 函数中的 `imageUrl` 参数

4. **成语消除首页** (`/subgames/future/index.js`)

   * 移除 `onShareAppMessage` 函数中的 `imageUrl` 参数

   * 移除 `onShareTimeline` 函数中的 `imageUrl` 参数

5. **七十二幻境首页** (`/subgames/72DreamlandPoetryTravel/index.js`)

   * 移除 \`on


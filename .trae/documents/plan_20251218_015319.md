1. **修复wx.getImageInfo参数错误**

   * 将wx.getImageInfo的参数从filePath改为src

   * 这是导致压缩失败的根本原因

2. **简化压缩流程**

   * 移除不必要的日志输出

   * 保持80%质量比的压缩设置

   * 确保只处理海报背景图（如果需要）

3. **测试修复后的压缩工具**

   * 验证海报背景图能否被正确压缩

   * 检查压缩质量是否为80%

   * 确保压缩后的图片能正常保存到相册

4. **预期结果**

   * 神兽海报背景图将被成功压缩为80%质量比

   * 压缩后的图片将保存在本地相册中

   * 压缩工具将不再出现"parameter.src should be String instead of Undefined"错误


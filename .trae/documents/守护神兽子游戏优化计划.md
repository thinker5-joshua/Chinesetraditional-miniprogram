### 守护神兽子游戏优化计划

**优化点1：首页脊兽列表区隐藏item.meaning**
- **文件**：`subgames/guardian-beast/index.wxss`
- **修改内容**：为首页列表中的`.info-description`和`.description-text`类添加`display: none`样式，隐藏描述文本

**优化点2：已抽取守护神兽展示区布局调整**
- **文件**：`subgames/guardian-beast/index.wxss`
- **修改内容**：
  1. 调整`.beast-main-content`的布局，设置`justify-content: flex-end`和适当的`padding-left`，使其整体向右移动
  2. 保持`.beast-image-large-container`居中显示
  3. 调整`.beast-info-large`的文本对齐方式为右对齐

**优化点3：减小操作按钮尺寸**
- **文件**：`subgames/guardian-beast/index.wxss`
- **修改内容**：
  1. 减小`.action-btn`的`padding`值
  2. 减小`.action-btn`的`font-size`值
  3. 减小`.action-btn`的`min-width`值
  4. 调整`.action-buttons`的`gap`值，确保按钮不超出边界

**优化点4：确保不超出右边界**
- **文件**：`subgames/guardian-beast/index.wxss`
- **修改内容**：
  1. 为`.extracted-beast-section`添加适当的内边距
  2. 确保所有元素的总宽度不超过父容器宽度
  3. 调整响应式设计，确保在不同尺寸的设备上都能正常显示

### 预期效果
1. 首页脊兽列表区不再显示描述文本，只显示名称、拼音和守护能力
2. 已抽取的守护神兽展示区整体向右移动，图形居中，信息靠右对齐
3. 操作按钮尺寸减小，不再超出框边界
4. 整体布局更加紧凑，视觉效果更好

### 实现方式
- 仅修改CSS样式文件，无需修改HTML结构或JavaScript逻辑
- 保持现有功能不变，只优化布局和视觉效果
- 确保代码的可维护性和扩展性
# 统一守护神兽模块分享功能

## 1. 为首页（index.js）添加分享功能
- 添加 `onShareAppMessage` 函数，用于分享给朋友
- 添加 `onShareTimeline` 函数，用于分享到朋友圈
- 分享标题："守护神兽 - 古建脊兽，守护灵契"
- 分享路径：`/subgames/guardian-beast/index`

## 2. 为抽取页面（draw.js）完善分享功能
- 添加 `onShareTimeline` 函数，用于分享到朋友圈
- 完善 `onShareAppMessage` 函数
- 分享标题："守护神兽抽取仪式"
- 分享路径：`/subgames/guardian-beast/draw`

## 3. 为结果页面（result.js）完善分享功能
- 添加 `onShareTimeline` 函数，用于分享到朋友圈
- 完善 `onShareAppMessage` 函数
- 分享标题："我抽到了${beast.name} - 守护神兽"
- 分享路径：`/subgames/guardian-beast/result?beastId=${beast.id}&proverb=${beast.selectedProverb}`

## 4. 为海报页面（poster.js）完善分享功能
- 添加 `onShareTimeline` 函数，用于分享到朋友圈
- 完善 `onShareAppMessage` 函数
- 移除或整合 `sharePoster` 函数，使其与标准分享机制一致
- 分享标题："我的守护神兽海报 - ${beast.name}"
- 分享路径：`/subgames/guardian-beast/poster?beastId=${beast.id}&proverb=${beast.selectedProverb}`

## 5. 验证修改
- 确保所有页面的分享功能都能正常工作
- 验证分享内容和路径是否正确
- 确保与其他模块的分享机制一致
我将按照以下步骤更新六十四卦中的象曰内容：

1. **创建卦名-象曰映射**：根据用户提供的表格，创建一个映射对象，将卦名的核心部分（如"乾"、"坤"）映射到对应的象曰内容
2. **遍历六十四卦数据**：读取现有的hexagrams-data.js文件，遍历其中的每一个卦
3. **提取核心卦名**：对于每个卦，提取其核心卦名（如从"乾卦"中提取"乾"，从"小畜卦"中提取"小畜"）
4. **更新象曰属性**：使用核心卦名在映射中查找对应的象曰内容，更新该卦的xiangYue属性
5. **保持其他属性不变**：确保只更新xiangYue属性，其他属性（如id、name、symbol、description等）保持不变
6. **保存更新后的数据**：将更新后的数据写回到hexagrams-data.js文件中

更新时需要注意：
- 所有64个卦都要正确匹配
- 准确按照用户提供的表格内容更新
- 保持数据文件的原有结构不变
# 每日一字调试代码清理计划

## 清理范围

### 1. 主入口文件 (`subgames/daily-character/index.js`)

**需要清理的调试代码**：
- 第74行：`console.log('读取到本地存储的难度模式:', savedDifficulties);`
- 第82-87行：旧格式转换的调试日志
- 第99行：`console.log('无本地存储，使用默认难度模式');`
- 第112行：`console.log('读取到本地存储的测试难度:', testDifficulty);`
- 第180-183行：加载汉字数据的调试代码
- 第491行：`console.error('获取小程序码图片失败:', error);`
- 第645行：`console.error('未知的难度等级:', level);`
- 第732-738行：过滤汉字的调试日志
- 第763行：`console.log('过滤后汉字数量:', filteredCharacters.length);`
- 第786-797行：错误原因类型转换的详细调试日志
- 第962行：`console.error('生成分享图片失败:', err);`
- 第978行：`console.error('绘制小程序码失败:', e);`
- 第990行：`console.error('小程序码图片加载失败，使用默认样式');`
- 第999行：`console.error('获取小程序码图片失败:', error);`
- 第1019行：`console.log('分享成功', res);`
- 第1022行：`console.log('分享失败', res);`

### 2. 列表页文件 (`subgames/daily-character/pages/list/list.js`)

**需要清理的调试代码**：
- 第49-52行：加载汉字数据的调试信息
- 第108-116行：详细的调试信息

### 3. 测试页文件 (`subgames/daily-character/pages/test/index.js`)

**需要清理的调试代码**：
- 第39-40行：选中字符的调试信息
- 第56-58行：每个字符相关词组的调试信息
- 第70-74行：生成测试题的调试信息

## 清理原则

1. **保留必要的错误处理**：保留 try-catch 结构，但移除 console 语句
2. **保留功能代码**：只清理调试日志，不影响核心功能
3. **保持代码整洁**：移除注释中标记为调试的代码
4. **确保代码可读性**：清理后代码应简洁明了

## 实现步骤

1. 依次清理每个文件中的调试代码
2. 从 `index.js` 开始，然后是 `list.js` 和 `test.js`
3. 仔细检查每个 console 语句，确保只清理调试用的语句
4. 保留必要的错误处理逻辑
5. 验证清理后的代码功能正常
6. 确认所有调试代码已清理完毕，准备发布
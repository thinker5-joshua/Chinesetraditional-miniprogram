# 统一修改二维码图片为 wyhd-minipro.png

## 任务描述

将六十四卦学习和每日一字中制作卡片时使用的二维码图片统一改为云存储上的 `wyhd-minipro.png` 文件。

## 修改内容

### 1. 每日一字（subgames/daily-character/index.js）

- 修改 `saveCard` 函数中的二维码图片：
  - 将 `cloudStorage.getImage('official-account-qr.png')` 改为 `cloudStorage.getImage('wyhd-minipro.png')`
  - 位置：第486行

- 修改 `generateShareImage` 函数中的二维码图片：
  - 将 `cloudStorage.getImage('official-account-qr.png')` 改为 `cloudStorage.getImage('wyhd-minipro.png')`
  - 位置：第971行

### 2. 六十四卦学习（subgames/64Hexagrams/index.js）

- 修改 `saveHexagramCard` 函数中的二维码图片：
  - 将 `cloudStorage.getImage('official-account-qr.png')` 改为 `cloudStorage.getImage('wyhd-minipro.png')`
  - 位置：第658行

### 3. 云存储配置（utils/cloudStorage.js）

- 添加 `wyhd-minipro.png` 到云存储配置中：
  - 在 `cloudFiles` 对象中添加对应的云文件路径
  - 在 `localFiles` 对象中添加对应的本地备选路径

## 注意事项

1. 确保云存储中存在 `wyhd-minipro.png` 文件
2. 保持错误处理逻辑不变，确保在图片加载失败时能正常降级到本地默认图片
3. 验证修改后的代码能正常运行

## 预期结果

所有制作卡片时使用的二维码图片都将统一为 `wyhd-minipro.png`，确保视觉一致性。
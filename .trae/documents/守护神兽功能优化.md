# 守护神兽功能优化

## 1. 脊兽详情页样式调整
- **文件**: `subgames/guardian-beast/knowledge/detail.wxml` 和 `subgames/guardian-beast/knowledge/detail.wxss`
- **修改内容**:
  - 将 `beast-name` 和 `beast-pinyin` 调整为一行显示，中间添加适当间距
  - 将 `rank` 移至信息框的右下角
  - 增大 `beast-beastname` 字体大小
  - 参考首页已抽取展示区的字体和颜色设置
  - 增大详细卡片中所有字体大小

## 2. 首页重新抽选功能优化
- **文件**: `subgames/guardian-beast/index.js`
- **修改内容**:
  - 调整 `goToDraw` 函数，点击重新抽取时立即重置抽取状态
  - 无论后续抽取结果如何，先回到未抽取状态
  - 成功抽取后写入新状态，中途退出保持未抽取状态

## 3. 首页脊兽列表样式调整
- **文件**: `subgames/guardian-beast/index.wxss`
- **修改内容**:
  - 减小 `beast-ability` 字体大小

## 4. 抽取页面背景图添加
- **文件**: `subgames/guardian-beast/draw.wxml` 和 `subgames/guardian-beast/draw.wxss`
- **修改内容**:
  - 在神兽背面卡背景上添加故宫图形文件
  - 设置背景图为淡化、虚化效果
  - 压缩质量60%，后续切换为jpg格式
  - 使用临时压缩按钮处理背景图

## 5. 抽取结果页样式调整
- **文件**: `subgames/guardian-beast/result.wxml`、`subgames/guardian-beast/result.wxss` 和 `subgames/guardian-beast/result.js`
- **修改内容**:
  - 标题区域排版参考脊兽详情页，name和pinyin一行显示
  - 增加显示神兽名称 `beast-beastname`
  - 增大下方内容区字体大小
  - 将"重新抽取"按钮改为"返回"，返回到首页
  - 将"生成海报"按钮改为"分享您的守护神兽"
  - 页面名称改为"您的专属守护神兽"

## 6. 海报生成优化
- **文件**: `subgames/guardian-beast/poster.js` 和 `subgames/guardian-beast/poster.wxml`
- **修改内容**:
  - 页面名称和标题改为"您的专属守护神兽"
  - 海报中显示"我的专属守护神兽"字眼
  - 以神兽形象为主体，图像采用圆圈造型
  - 确保海报包含名称、神兽名称、守护能力、契主特质
  - 优化排版，确保美观
  - 调整字体大小和颜色，保持与其他页面风格一致

## 实现思路
1. 先修改各页面的HTML结构，调整元素布局
2. 然后修改CSS样式，实现视觉效果
3. 最后调整JavaScript逻辑，实现功能优化
4. 使用临时压缩按钮处理背景图
5. 确保所有修改保持一致的设计风格，参考首页已抽取展示区的样式
6. 注意背景图的处理，确保其淡化效果不影响主要内容显示
7. 测试各功能点，确保逻辑正确，用户体验流畅
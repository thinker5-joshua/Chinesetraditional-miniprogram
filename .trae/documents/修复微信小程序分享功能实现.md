1. **修复分享功能实现方式**：

   * 移除直接调用 `wx.shareAppMessage` 和 `wx.shareTimeline` 的代码

   * 恢复分享按钮的 `open-type="share"` 属性

   * 确保页面中有正确的 `onShareAppMessage` 和 `onShareTimeline` 生命周期函数

2. **调整分享逻辑**：

   * 对于菜根谭摘录页面，保留分享选择弹窗，但修改分享到朋友和朋友圈的实现方式

   * 对于守护神兽海报页面，同样调整分享实现方式

3. **实现正确的分享机制**：

   * 当用户点击分享按钮时，根据用户选择设置分享类型

   * 在生命周期函数中根据分享类型返回不同的分享内容

4. **测试分享功能**：

   * 测试分享给朋友功能

   * 测试分享到朋友圈功能

   * 确保生成分享图片功能正常

5. **保持分享选择弹窗的设计**：

   * 用户点击分享按钮后，先显示选择弹窗

   * 根据用户选择，设置分享类型并触发微信分享


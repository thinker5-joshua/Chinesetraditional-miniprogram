### 替换诗词不足地点并添加新地点诗词

#### 1. 替换洞天数据
- 更新 `caves_data.js` 文件，将以下三个地点替换为新地点：
  - 乐山大佛（id: 40）→ 趵突泉
  - 都江堰（id: 69）→ 鹳雀楼
  - 泉州开元寺（id: 72）→ 雁门关
- 为新地点添加完整的描述、经纬度等信息

#### 2. 删除旧地点诗词
- 运行清理脚本，删除乐山大佛、都江堰和泉州开元寺的所有诗词
- 确保诗词数据中不再包含这些地点的诗词

#### 3. 为新地点查找诗词
- 使用 `find_location_poems.js` 脚本为三个新地点查找相关诗词：
  - 趵突泉：查找与趵突泉、济南相关的诗词
  - 鹳雀楼：查找与鹳雀楼、永济相关的诗词
  - 雁门关：查找与雁门关、代县相关的诗词

#### 4. 批量添加诗词
- 使用 `add_poems.js` 脚本为三个新地点批量添加诗词
- 确保每个地点至少添加3首诗词

#### 5. 验证诗词有效性
- 运行 `comprehensive_validate.js` 脚本验证所有诗词的有效性
- 检查每个地点的诗词数量是否达到3-5首

#### 6. 最终清理和确认
- 运行 `clean_poems_data.js` 脚本清理诗词数据
- 确保所有诗词ID连续，无重复
- 最终验证所有地点的诗词数量和有效性

### 预期结果
- 更新后的 `caves_data.js` 包含72个有效地点
- 更新后的 `poems_data.js` 包含所有有效诗词
- 每个地点至少有3首有效诗词
- 生成完整的验证报告

### 执行顺序
1. 替换洞天数据
2. 删除旧地点诗词
3. 查找新地点诗词
4. 批量添加诗词
5. 验证诗词有效性
6. 最终清理和确认
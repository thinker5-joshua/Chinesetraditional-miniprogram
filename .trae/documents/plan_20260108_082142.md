# 任务分析

根据用户需求，我需要完成以下任务：

1. **统一使用data-optimized.js文件**：
   - 替换所有使用data.js的地方，改为使用data-optimized.js
   - 确保所有相关文件都正确引用新的数据文件

2. **清理测试和调试代码及文件**：
   - 移除测试和调试代码
   - 删除不需要的测试文件
   - 清理调试日志和临时文件

# 解决方案

## 1. 统一数据文件
- **文件**：`subgames/daily-character/pages/test/index.js`
- **修改点**：将 `require('../../data.js')` 改为 `require('../../data-optimized.js')`

- **文件**：`subgames/daily-character/pages/card/index.js`
- **修改点**：将 `require('../../data.js')` 改为 `require('../../data-optimized.js')`

## 2. 清理测试和调试代码
- **文件**：`subgames/daily-character/pages/test/index.js`
- **修改点**：移除调试日志 `console.log` 语句

- **文件**：`subgames/daily-character/index.js`
- **修改点**：移除测试和调试代码

## 3. 清理不需要的文件
- **删除文件**：`subgames/daily-character/data.js`（已不再使用）
- **删除文件**：各种测试和处理脚本文件，如 `process-characters.js`、`enhance-data.js`、`generate-list.js` 等
- **删除文件**：临时数据文件，如 `processed-characters.json`
- **删除文件**：各种 `remove-*.js` 文件，用于测试和调试

# 预期效果

- 所有每日一字相关功能统一使用 data-optimized.js 文件
- 代码干净整洁，没有测试和调试代码
- 项目结构清晰，只保留发布所需的文件
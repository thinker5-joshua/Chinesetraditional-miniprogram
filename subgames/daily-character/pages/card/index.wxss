/* 全局样式 */
.container {
  min-height: 100vh;
  background-color: #f8f6f0;
  padding: 20rpx;
  box-sizing: border-box;
}

/* 卡片容器 */
.card-container {
  background: white;
  border-radius: 20rpx;
  padding: 50rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  border: 2rpx solid #e8e4d8;
}

/* 汉字展示区 */
.character-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 50rpx;
  padding: 50rpx 0;
  border-bottom: 2rpx solid #f0f0f0;
}

.character {
  font-size: 220rpx;
  font-weight: bold;
  color: #8B4513;
  margin-bottom: 50rpx;
}

.pronunciation-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
}

.correct-pronunciation {
  font-size: 44rpx;
  color: #333;
  font-weight: bold;
}

.wrong-pronunciation {
  font-size: 36rpx;
  color: #ff6b6b;
}

/* 通用区块样式 */
.detail-section,
.phrases-section,
.wrong-phrases-section {
  margin-bottom: 60rpx;
  padding-bottom: 40rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.section-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #8B4513;
  margin-bottom: 25rpx;
  padding-left: 12rpx;
  border-left: 10rpx solid #8B4513;
}

/* 详细解析区 */
.explanation {
  font-size: 36rpx;
  color: #666;
  line-height: 1.7;
  text-align: justify;
}

/* 词组区 */
.phrases-grid {
  display: flex;
  flex-wrap: wrap;
  margin: -10rpx;
}

.phrase-item {
  background-color: #f5f5f5;
  padding: 25rpx 35rpx;
  border-radius: 30rpx;
  font-size: 34rpx;
  color: #555;
  margin: 10rpx;
}

/* 常见错误区 */
.wrong-phrases-list {
  display: flex;
  flex-direction: column;
}

.wrong-phrase-item {
  background-color: #fff3f3;
  padding: 25rpx;
  border-radius: 20rpx;
  font-size: 36rpx;
  color: #d9534f;
  border: 1rpx solid #ffdddd;
  margin-bottom: 20rpx;
}

/* 操作按钮区 */
.action-buttons {
  display: flex;
  gap: 25rpx;
  margin-top: 40rpx;
}

.action-btn {
  flex: 1;
  height: 100rpx;
  border-radius: 50rpx;
  font-size: 36rpx;
  font-weight: bold;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
}

.reload-btn {
  background-color: #D2B48C;
  color: #8B4513;
}

.test-btn {
  background-color: #8B4513;
  color: white;
}

/* 加载状态 */
.loading {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 500rpx;
  font-size: 32rpx;
  color: #666;
}
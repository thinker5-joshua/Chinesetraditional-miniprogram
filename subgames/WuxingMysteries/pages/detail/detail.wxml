<!-- 页面容器 -->
<view class="page-container">
  <!-- 固定头部区域 -->
  <view class="header-section">
    <view class="header-main">
      <view class="element-symbol" style="background: {{elementInfo.color || '#667eea'}};">
        <text class="symbol-text">{{elementInfo.symbol || elementInfo.title}}</text>
      </view>
      <view class="element-title-container">
        <view class="element-name">{{elementInfo.element}}</view>
        <view class="element-wuxing" wx:if="{{elementInfo.wuxing && tabType !== 'wuxing'}}">
          <text class="wuxing-label">五行：</text>
          <text class="wuxing-value">{{elementInfo.wuxing}}</text>
        </view>
      </view>
    </view>
  </view>
  
  <!-- 滚动内容区域 -->
  <scroll-view class="content-scroll" scroll-y="true">
    <!-- 数据加载状态提示 -->
    <view wx:if="{{!elementInfo.element}}" class="loading-container">
      <view class="loading-icon">🔄</view>
      <text class="loading-text">正在加载数据...</text>
    </view>
    
    <view wx:else class="content-wrapper">

      <!-- 基本描述 -->
      <view class="description-section">
        <view class="card-header">
          <text class="section-title">核心特征</text>
          <view class="decorative-line"></view>
        </view>
        <view class="description-text">{{elementInfo.description}}</view>
      </view>

      <!-- 基本属性 -->
      <view class="properties-section" wx:if="{{elementInfo.properties && elementInfo.properties.length > 0}}">
        <view class="card-header">
          <text class="section-title">主要特性</text>
          <view class="decorative-line"></view>
        </view>
        <view class="attribute-list">
          <view class="attribute-item" wx:for="{{elementInfo.properties}}" wx:key="*this">
            <text class="attribute-text">{{item}}</text>
          </view>
        </view>
      </view>

      <!-- 五行关系 - 完整显示相生相克 -->
      <view class="wuxing-section" wx:if="{{elementInfo.wuxingRelationships}}">
        <view class="card-header">
          <text class="section-title">五行关系</text>
          <view class="decorative-line"></view>
        </view>
        <view class="wuxing-relations">
          <!-- 相生关系 -->
          <view class="relation-section">
            <view class="relation-label">相生：</view>
            <view class="relation-content-two-lines">
              <view class="relation-line">{{elementInfo.wuxingRelationships.shengLine1}}</view>
              <view class="relation-line">{{elementInfo.wuxingRelationships.shengLine2}}</view>
            </view>
          </view>
          <!-- 相克关系 -->
          <view class="relation-section">
            <view class="relation-label">相克：</view>
            <view class="relation-content-two-lines">
              <view class="relation-line">{{elementInfo.wuxingRelationships.keLine1}}</view>
              <view class="relation-line">{{elementInfo.wuxingRelationships.keLine2}}</view>
            </view>
          </view>
        </view>
      </view>

      <!-- 自然对应 -->
      <view class="nature-section" wx:if="{{elementInfo.season || elementInfo.direction || elementInfo.planet || elementInfo.number}}">
        <view class="card-header">
          <text class="section-title">自然对应</text>
          <view class="decorative-line"></view>
        </view>
        <view class="nature-grid">
          <view class="nature-item" wx:if="{{elementInfo.season}}">
            <view class="nature-label">季节</view>
            <view class="nature-value">{{elementInfo.season}}</view>
          </view>
          <view class="nature-item" wx:if="{{elementInfo.direction}}">
            <view class="nature-label">方位</view>
            <view class="nature-value">{{elementInfo.direction}}</view>
          </view>
          <view class="nature-item" wx:if="{{elementInfo.planet}}">
            <view class="nature-label">行星</view>
            <view class="nature-value">{{elementInfo.planet}}</view>
          </view>
          <view class="nature-item" wx:if="{{elementInfo.number}}">
            <view class="nature-label">数理</view>
            <view class="nature-value">{{elementInfo.number}}</view>
          </view>
        </view>
      </view>

      <!-- 养生建议 -->
      <view class="health-section" wx:if="{{elementInfo.healthTips && elementInfo.healthTips.length > 0}}">
        <view class="card-header">
          <text class="section-title">养生建议</text>
          <view class="decorative-line"></view>
        </view>
        <view class="health-tips">
          <view class="health-tip" wx:for="{{elementInfo.healthTips}}" wx:key="*this">
            {{item}}
          </view>
        </view>
      </view>

      <!-- 文化意义 -->
      <view class="cultural-section" wx:if="{{elementInfo.cultural && elementInfo.cultural.length > 0}}">
        <view class="card-header">
          <text class="section-title">文化意义</text>
          <view class="decorative-line"></view>
        </view>
        <view class="cultural-content">
          <view class="cultural-item" wx:for="{{elementInfo.cultural}}" wx:key="*this">
            {{item}}
          </view>
        </view>
      </view>


    </view>
  </scroll-view>

  <!-- 底部固定返回按钮 -->
  <view class="footer-section">
    <view class="back-button" bindtap="goBack">
      <text class="back-text">返回</text>
    </view>
  </view>
</view>